#include <stdio.h>
#include "main.h"
int add(int, int);
/**
 * _abs - prints absolute of number
 * @n: number
 * @m: number
 *
 * Description: does my stuff
 * Return: 1 or 0
 */
int add(int n, int m)
{
  return (n + m);
}
